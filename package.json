{
  "name": "trooper",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous engineering agent — ingest feedback, write code, open PRs. BYOK.",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-org/trooper"
  },
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "db:generate": "pnpm --filter @trooper/db generate",
    "db:migrate": "pnpm --filter @trooper/db migrate"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "prettier": "^3.2.0",
    "eslint": "^8.57.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
